<!DOCTYPE html>
<html lang="en" data-bs-theme="light">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - EventsMS</title>

    <!-- Google Font -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <!-- Bootstrap + Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet" />

    <style>
        body {
            font-family: 'Inter', sans-serif;
            background: #f8fafc;
            color: #0f172a;
            transition: 0.3s;
        }

        /* Navbar */
        .navbar {
            backdrop-filter: blur(12px);
            background: rgba(255,255,255,0.9);
            border-bottom: 1px solid #e2e8f0;
        }

        .nav-link {
            font-weight: 500;
            color: #475569 !important;
        }

            .nav-link:hover,
            .nav-link.active {
                color: #3b82f6 !important;
            }

        main {
            min-height: 75vh;
            padding: 2rem 0;
        }

        footer {
            background: #ffffff;
        }

        /* DARK MODE */
        [data-bs-theme="dark"] {
            background: #0f172a;
            color: #e2e8f0;
        }

            [data-bs-theme="dark"] .navbar {
                background: rgba(15,23,42,0.9) !important;
                border-bottom: 1px solid #334155;
            }

            [data-bs-theme="dark"] footer {
                background: #020617;
            }

            [data-bs-theme="dark"] .nav-link {
                color: #cbd5f5 !important;
            }

                [data-bs-theme="dark"] .nav-link.active {
                    color: #60a5fa !important;
                }
    </style>

    @await RenderSectionAsync("Styles", required: false)
</head>

<body>

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg sticky-top shadow-sm">
        <div class="container">

            <a class="navbar-brand fw-bold d-flex align-items-center gap-2" href="#">
                <i class="bi bi-calendar-event-fill text-primary"></i>
                EventsMS
            </a>

            <button class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#nav">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="nav">

                <!-- Menu -->
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item">
                        <a asp-action="Index" class="nav-link active">All Events</a>
                    </li>
                    <li class="nav-item">
                        <a asp-controller="Home" asp-action="Index" class="nav-link">My Events</a>
                    </li>
                </ul>

                <!-- Right -->
                <div class="d-flex gap-2 align-items-center">

                    @if (User.Identity?.IsAuthenticated == true)
                    {
                        <form asp-controller="Account" asp-action="Logout" method="post">
                            <button class="btn btn-danger btn-sm">Logout</button>
                        </form>
                    }
                    else
                    {
                        <a asp-controller="Account" asp-action="Login" class="btn btn-outline-primary">Login</a>
                        <a asp-controller="Account" asp-action="Register" class="btn btn-primary">Register</a>
                    }

                    <!-- Theme Toggle -->
                    <button id="themeToggle"
                            class="btn btn-light border rounded-circle p-2"
                            style="width:42px;height:42px;">
                        <i class="bi bi-moon-stars-fill" id="themeIcon"></i>
                    </button>

                </div>
            </div>
        </div>
    </nav>

    <!-- Body -->
    <main class="container">
        @RenderBody()
    </main>

    <!-- Footer -->
    <footer class="border-top mt-5 py-4">
        <div class="container text-center text-muted">
            © @DateTime.Now.Year EventsMS
        </div>
    </footer>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <!-- DARK/LIGHT SCRIPT -->
    <script>
        const themeToggle = document.getElementById('themeToggle');
        const themeIcon = document.getElementById('themeIcon');

        let currentTheme = localStorage.getItem('theme') || 'light';

        document.documentElement.setAttribute('data-bs-theme', currentTheme);

        themeIcon.className = currentTheme === 'dark'
            ? 'bi bi-sun-fill'
            : 'bi bi-moon-stars-fill';

        themeToggle.addEventListener('click', () => {
            currentTheme = currentTheme === 'light' ? 'dark' : 'light';

            document.documentElement.setAttribute('data-bs-theme', currentTheme);
            localStorage.setItem('theme', currentTheme);

            themeIcon.className = currentTheme === 'dark'
                ? 'bi bi-sun-fill'
                : 'bi bi-moon-stars-fill';
        });
    </script>

    @await RenderSectionAsync("Scripts", required: false)

</body>
</html>



@* <!DOCTYPE html>
<html lang="en" data-bs-theme="light">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - EventsMS</title>

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:opsz,wght@14..32,300;14..32,400;14..32,500;14..32,600;14..32,700;14..32,800&display=swap" rel="stylesheet">

    <!-- Bootstrap + Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet" />

    @await RenderSectionAsync("Styles", required: false)
</head>
<body>

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg sticky-top">
        <div class="container">
            <a class="navbar-brand d-flex align-items-center gap-2" href="#">
                <i class="bi bi-calendar-event-fill fs-2 text-primary"></i>
                <span>EventsMS</span>
            </a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mx-auto gap-1">
                    <li class="nav-item">
                        <a asp-action="Index" class="nav-link active">All Events</a>
                    </li>
                    <li class="nav-item">
                        <a asp-controller="Home" asp-action="Index" class="nav-link">My Events</a>
                    </li>
                    <li class="nav-item">
                        <a asp-controller="Home" asp-action="Index" class="nav-link">Upcoming</a>
                    </li>
                </ul>

                <div class="d-flex align-items-center gap-3">
                    @if (User.Identity?.IsAuthenticated == true)
                    {
                        <div class="dropdown">
                            <a class="d-flex align-items-center text-decoration-none dropdown-toggle" href="#" data-bs-toggle="dropdown">
                                <i class="bi bi-person-circle fs-3 text-secondary"></i>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end shadow">
                                <li><a asp-action="Index" asp-controller="Dashboard" class="dropdown-item"><i class="bi bi-person me-2"></i>Profile</a></li>
                                <li><a class="dropdown-item" href="#"><i class="bi bi-gear me-2"></i>Settings</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li>
                                    <form asp-controller="Account" asp-action="Logout" method="post">
                                        <button type="submit" class="dropdown-item text-danger">Logout</button>
                                    </form>
                                </li>
                            </ul>
                        </div>
                    }
                    else
                    {
                        <a asp-controller="Account" asp-action="Login" class="btn btn-outline-primary rounded-pill px-4">Login</a>
                        <a asp-controller="Account" asp-action="Register" class="btn btn-primary rounded-pill px-4">Register</a>
                    }

                    <button id="themeToggle" class="btn btn-outline-secondary rounded-circle p-2" style="width:42px;height:42px;">
                        <i class="bi bi-moon-stars-fill"></i>
                    </button>
                </div>
            </div>
        </div>
    </nav>

    <!-- Page Content -->
    <main>
        @RenderBody()
    </main>

    <!-- Footer -->
    <footer class="footer mt-5 py-5 border-top">
        <div class="container">
            <div class="row gy-5">
                <div class="col-lg-5">
                    <div class="d-flex align-items-center gap-2 mb-3">
                        <i class="bi bi-calendar-event-fill fs-2 text-primary"></i>
                        <h4 class="mb-0 fw-bold">EventsMS</h4>
                    </div>
                    <p class="text-muted">Connecting people through meaningful events and unforgettable experiences.</p>
                </div>
                <div class="col-lg-2 col-md-6">
                    <h5>Platform</h5>
                    <a href="#" class="d-block mb-2 text-decoration-none text-muted">Browse Events</a>
                    <a href="#" class="d-block mb-2 text-decoration-none text-muted">Create Event</a>
                    <a href="#" class="d-block mb-2 text-decoration-none text-muted">Upcoming Events</a>
                </div>
                <div class="col-lg-2 col-md-6">
                    <h5>Company</h5>
                    <a href="#" class="d-block mb-2 text-decoration-none text-muted">About Us</a>
                    <a href="#" class="d-block mb-2 text-decoration-none text-muted">Careers</a>
                    <a href="#" class="d-block mb-2 text-decoration-none text-muted">Blog</a>
                </div>
                <div class="col-lg-3">
                    <h5>Support</h5>
                    <a href="#" class="d-block mb-2 text-decoration-none text-muted">Help Center</a>
                    <a href="#" class="d-block mb-2 text-decoration-none text-muted">Terms of Service</a>
                    <a href="#" class="d-block mb-2 text-decoration-none text-muted">Privacy Policy</a>
                </div>
            </div>
            <hr class="my-4">
            <div class="text-center text-muted small">
                &copy; @DateTime.Now.Year EventsMS. All rights reserved.
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html> *@